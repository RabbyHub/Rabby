# Rabby Claude Entry

For Claude-style agents, start with `.claude/SKILL.md`. It explains how to discover and load repo-local skills without duplicating skill bodies under `.claude/`.

Everything below is shared with all agents and is maintained in `AGENTS.md`. Do not copy it here — edit `AGENTS.md` so both entry points stay in sync.

@AGENTS.md
