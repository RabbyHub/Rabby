import { useWallet } from '@/ui/utils';
import { NFTDetail } from '@rabby-wallet/rabby-api/dist/types';
import { useRequest } from 'ahooks';

export const useCalcBestOfferPrice = (nftDetail: NFTDetail) => {};
