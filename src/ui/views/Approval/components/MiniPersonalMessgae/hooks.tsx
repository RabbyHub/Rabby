import { useMemo } from 'react';
import { MiniPersonalMessage } from './useBatchPersonalMessageTask';
import { DrawerProps } from 'antd';

// export const useMiniPersonalMessage = ({
//   txs,
// }: {
//   txs: MiniPersonalMessage[];
//   onResolve?: (p: string[]) => void;
//   onReject?: () => void;
//   getContainer?: DrawerProps['getContainer'];
//   showSignBtn?: boolean;
// }) => {
//   const params = useMemo(() => txs, [txs]);

// };
