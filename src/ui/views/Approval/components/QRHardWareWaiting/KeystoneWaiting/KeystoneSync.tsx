import React from 'react';
import { useAsyncRetry } from 'react-use';
import { useWallet } from 'ui/utils';

export const KeystoneSync = () => {
  // const wallet = useWallet();
  // wallet.requestKeyring()

  return <div>KeystoneSync</div>;
};
