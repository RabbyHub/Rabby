import { keyringService } from 'background/service';
import { jsonFile } from './keyring.data';

describe('eth service test', () => {
  test('import JSON file', async () => {
    // let simpleKeyring = keyringService.getKeyringsByType('Simple Key Pair')[0];
    // expect(simpleKeyring).toBeUndefined();
    // await keyringService.importJson(jsonFile.content, jsonFile.password);
    // simpleKeyring = keyringService.getKeyringByType('Simple Key Pair');
    // expect(simpleKeyring).toBeDefined();
  });
});
